import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Crypto Indices Dashboard - Real-time Cryptocurrency Tracking",
  description: "Professional cryptocurrency indices dashboard with real-time prices, 30-day historical data, technical indicators (SMA, RSI), and smart caching. Built with Next.js, React, and TypeScript.",
  keywords: ["cryptocurrency", "crypto", "bitcoin", "ethereum", "trading", "dashboard", "indices", "technical analysis", "real-time prices"],
  authors: [{ name: "Leqi" }],
  viewport: "width=device-width, initial-scale=1",
  themeColor: "#3b82f6",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
